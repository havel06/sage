raylib_lib = meson.get_compiler('cpp').find_library('raylib', dirs : meson.source_root() / 'vendor/raylib/lib/')
raylib_inc = include_directories('../vendor/raylib/include')

vendor_inc = [raylib_inc]
